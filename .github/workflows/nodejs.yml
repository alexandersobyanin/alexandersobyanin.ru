name: Node CI

on:
  push:
    branches:
      - react

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@master

    - name: Build and Deploy
      uses: JamesIves/github-pages-deploy-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        BASE_BRANCH: react
        BRANCH: master
        FOLDER: build
        BUILD_SCRIPT: npm install && npm run build
        CNAME: alexandersobyanin.ru
        REACT_APP_FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
        REACT_APP_FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
        REACT_APP_FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
        REACT_APP_REACT_APP_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
        REACT_APP_FIREBASE_PUBLIC_VAPID_KEY: ${{ secrets.FIREBASE_PUBLIC_VAPID_KEY }}
        REACT_APP_REACT_TEST_GITHUB: ${{ secrets.REACT_TEST_GITHUB }}
